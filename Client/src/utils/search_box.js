// BACKGROUND COLOUR

$(document).ready(function getStylesheet() {
      // var currentTime = new Date().getHours();
      // if (0 <= currentTime&&currentTime < 5) {
      //  $("body").append("<link rel='stylesheet' href='../src/day.css' type='text/css'>");
      // }
      // if (5 <= currentTime&&currentTime < 11) {
      //  $("body").append("<link rel='stylesheet' href='../src/day.css' type='text/css'>");
      // }
      // if (11 <= currentTime&&currentTime < 16) {
      //  $("body").append("<link rel='stylesheet' href='../src/night.css' type='text/css'>");
      // }
      // if (16 <= currentTime&&currentTime < 22) {
      //  $("body").append("<link rel='stylesheet' href='../src/night.css' type='text/css'>");
      // }
      // if (22 <= currentTime&&currentTime <= 24) {
      //  $("body").append("<link rel='stylesheet' href='../src/night.css' type='text/css'>");
      // }

      // whenever press enter when search for key word, wil add class display:inline-block to the div overlay

      // $(".search-input").on('keypress',function(event){
      //   if (event.which == 13){
      //     let userInput = $(".search-input").val();
      //     $("#overlay").css("display", "inline-block");
      //     $(".search-wrapper").removeClass("active");
      //     // $(".search-input").append("Type to search");
      //   }
      // })

      // POPUP PAGE 
    // Initialize Variables
    var closePopup = document.getElementById("popupclose");
    var overlay = document.getElementById("overlay");
    var popup = document.getElementById("popup");

       // Close Popup Event
  //  closePopup.onclick = function() {
  //      overlay.style.display = 'none';
  //      popup.style.display = 'none';
  //  };
})




